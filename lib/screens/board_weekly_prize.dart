import 'package:flutter/material.dart';

class BoardWeeklyPrizesScreen extends StatelessWidget {
  const BoardWeeklyPrizesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Board Weekly Prizes'),
      ),
      body: const Center(
        child: Text('Board Weekly Prizes'),
      ),
    );
  }
}
