import 'package:flutter/material.dart';

class FPLContestScreen extends StatelessWidget {
  const FPLContestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FPL Contest'),
      ),
      body: const Center(
        child: Text('FPL Contest'),
      ),
    );
  }
}
